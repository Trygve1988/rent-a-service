<%= form_with(model: sheep) do |form| %>
  <% if sheep.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sheep.errors.count, "error") %> prohibited this sheep from being saved:</h2>

      <ul>
        <% sheep.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control"  %>
  </div>

  <div>
    <%= form.check_box :hungry, style: "display:inline" %>
    <%= form.label :hungry, style: "display: inline" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
